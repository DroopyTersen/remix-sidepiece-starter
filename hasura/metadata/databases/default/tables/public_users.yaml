table:
  name: users
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - name
        - photo
      filter: {}
  - role: user
    permission:
      columns:
        - created_at
        - id
        - name
        - photo
        - updated_at
      filter: {}
update_permissions:
  - role: user
    permission:
      columns:
        - created_at
        - email
        - name
        - photo
        - updated_at
      filter:
        id:
          _eq: X-Hasura-User-Id
      check: null
delete_permissions:
  - role: user
    permission:
      filter:
        id:
          _eq: X-Hasura-User-Id
