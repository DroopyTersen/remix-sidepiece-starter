table:
  name: teams
  schema: public
object_relationships:
  - name: org
    using:
      foreign_key_constraint_on: org_id
array_relationships:
  - name: members
    using:
      foreign_key_constraint_on:
        column: team_id
        table:
          name: team_users
          schema: public
